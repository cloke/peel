# Local RAG Model Setup (Deprecated)

This guide previously documented the CodeBERT → Core ML conversion pipeline. The CodeBERT tooling and catalog have been removed.

If you need to set up Local RAG now, use prebuilt Core ML artifacts and place them in the app support directory. Refer to the current
RAG plan for the active approach.